# Keep these ranges synchronized with pyproject.toml. pyproject.toml is the
# authoritative package metadata used by CI and editable installs.
beautifulsoup4>=4.12,<5.0
jsonschema[format]>=4.23,<5.0
markdown-it-py>=3.0,<5.0
pydantic>=2.7,<3.0
PyYAML>=6.0,<7.0
rich>=13.7,<15.0
typer>=0.12,<1.0

# Development dependencies
pytest>=8.0,<9.0
ruff>=0.6,<1.0
