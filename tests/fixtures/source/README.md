# Synthetic source fixtures

Files in this directory provide harmless, deterministic source context for normalizer tests.
They are not copied from the pinned Juice Shop repository and must not contain real credentials,
scanner output, or curated security evidence.

The directory below `juice-shop/` is passed explicitly as `--source-root`; fixture line numbers
must remain aligned with the matching synthetic scanner locations.
